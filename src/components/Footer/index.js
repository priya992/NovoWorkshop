import React from 'react';
import style from './styles.scss'

const Footer = () => {
  return (
    <div className={style.footerSection}>Footer Section</div>
  )
}

export default React.memo(Footer);
